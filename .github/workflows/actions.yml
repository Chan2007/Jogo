name: Build

on:
  push:
    branches:
      - "**"
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2022

    strategy:
      matrix:
        config: [Debug, Release]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Qt 5.15.2
        uses: jurplel/install-qt-action@v4
        with:
          version: 5.15.2
          arch: win64_msvc2019_64

      - name: Configure CMake
        shell: cmd
        run: >
          cmake -S . -B build
          -G "Visual Studio 17 2022"
          -A x64
          -T v142
          -DCMAKE_PREFIX_PATH="%Qt5_DIR%"

      - name: Build (${{ matrix.config }})
        shell: cmd
        run: cmake --build build --config ${{ matrix.config }}

      - name: Verify executable
        shell: cmd
        run: |
          if exist build\${{ matrix.config }}\Jogo.exe (
              echo Jogo.exe criado com sucesso.
          ) else (
              echo ERRO: Jogo.exe nao foi encontrado.
              exit /b 1
          )

      - name: Upload build
        uses: actions/upload-artifact@v4
        with:
          name: Jogo-${{ matrix.config }}
          path: build/${{ matrix.config }}/

      #- name: Generate Coverage
      #  shell: cmd
      #  run: >
      #    "C:\Program Files\OpenCppCoverage\OpenCppCoverage.exe"
      #    --sources="%CD%\src"
      #    --export_type=cobertura:build\coverage.xml
      #    --
      #    build\Debug\Jogo.exe

      #- name: Upload to Codecov
      #  uses: codecov/codecov-action@v5
      #  with:
      #    token: ${{ secrets.CODECOV_TOKEN }}
      #    files: build/coverage.xml
      #    fail_ci_if_error: true
